# LaTeXGenerator
A BetterDiscord plugin that generates LaTeX code. It uses the [Equation Render](https://latex.codecogs.com/) api to render the LaTeX code into an image. It also provides a preview of the rendered image.

![](assets/preview.gif)
